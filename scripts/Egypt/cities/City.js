export const egyptCity = (cityObj) => {
	return `<p>${cityObj.city}</p>`
  }

  // i am an export (CHINA) - I have  a function that formats into a paragraph


  // this data is what will display on the DOM itself